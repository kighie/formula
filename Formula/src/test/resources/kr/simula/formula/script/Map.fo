
/* System out wrapping */
fndef println(object value) { 
	if( not skipPrint ){
		System.out.println(value);
	}
} 


map simpleMap <- {
	a:1,
	b:2,
	c:3,
	"text key" : 5
};


map simpleMap2 <- {
};

text[] textArray <- ['a','b'];


println( textArray[1] );

println( "*** simpleMap['text key'] : " & simpleMap["text key"] );

println( "KEYS : " & simpleMap.keySet() );

foreach(object a in simpleMap.keySet() ) {
	println("key:" & a);
}

foreach(object a in simpleMap.values() ) {
	println("value:" & a);
}

println( 'SUM values : ' & SUM(simpleMap.values()) );
println( 'AVERAGE values : ' & AVERAGE(simpleMap.values()) );
